# Blender 插件库管理器修复文档

> **文档范围**：本文档记录 **1.0.x** 系列的功能现状、已确认问题与修复验收，其中的基线数字、面板/操作符清单和界面入口描述均以该系列为准，**不代表当前版本**。
>
> 当前版本（2.1.0）的架构与界面入口见 `docs/PLUGIN_MANAGER_ARCHITECTURE_AUDIT.md` 与 `README.md`。其中与本文档差异较大的部分：
> - 兼容性实测已改为不阻塞界面、可取消的分步任务（不再是单次阻塞遍历）；
> - 3D 视图顶部栏入口已移除，侧栏的「迁移与启动控制」子面板已移除，低频维护入口集中在插件偏好设置；
> - 多电脑共享库、按设备配置、schema 2 数据分层取代了 1.0.x 的单一配置方式。

## 1. 文档目的

本文档记录插件库管理器 1.0.1 的功能现状、已确认问题、根因、修复内容和验收结果，供后续维护与回归测试使用。

本次结论来自：

- 工作区源码静态审查；
- Blender 5.2.1 LTS 实机检查；
- Blender MCP 只读检查与临时目录边界测试；
- 工作区源码和 Blender 实际安装源码的一致性校验。

## 2. 当前功能与运行基线

### 2.1 功能范围

插件目前提供以下能力：

1. 集中管理传统 Blender 插件和 Blender 扩展插件；
2. 将插件库挂载为 Blender 脚本目录和扩展仓库；
3. 从文件夹、ZIP、投放区导入插件；
4. 扫描并收编 Blender 中已有的插件；
5. 管理分类、别名、备注、标签、收藏和自启标记；
6. 单个或批量启用、停用插件；
7. 根据 manifest 或 `bl_info` 判断 Blender 版本兼容性；
8. 逐个试加载插件并记录失败原因和注册残留；
9. 浏览 Blender 官方扩展商店，安装和更新扩展；
10. 在 3D 视口侧边栏和标题栏中提供快捷管理入口；
11. 生成投放区扫描报告和错误日志。

### 2.2 实机基线

审计时的运行状态：

- Blender：5.2.1 LTS；
- 插件：1.0.1；
- 插件库记录：152 条；
- 传统插件：81 个；
- 扩展插件：71 个；
- 缺失插件目录：0；
- 数据库启用状态与 Blender 实际状态不一致：0；
- 已记录的 manifest 问题与实时检查不一致：0；
- 5 个主要面板均可绘制；
- 45 个操作符均已注册；

## 8. 1.0.1 实际修复与验收结果

### 8.1 已落地修复

- 删除插件改为“先移入回收站”；移动失败时保留源目录和数据库记录，不再回退为不可恢复删除。
- 插件库普通挂载不再隐式接管 `blender_org` 或打开联网；只有显式执行“统一商店”才改写官方仓库，并保存可恢复快照。
- 同版本插件的 manifest / `bl_info` 元数据变化会触发刷新；替换 ZIP 使用临时目录、备份和回滚，避免半更新。
- Blender 模块索引和挂载状态缓存改为路径/启用模块签名；同时清理已删除路径的 Blender 假模块和 `sys.modules` 残留，避免同名插件指向旧目录。
- 数据库缓存使用深拷贝隔离，未保存的内存修改不会污染后续读取。
- 兼容性检查真实执行启用插件的禁用/恢复流程，并记录恢复失败；列表显示恢复错误。
- 测试脚本会检查 Blender 进程退出码、JSON 汇总和失败数；最终检查排除 `.blender_ext` 元数据目录误报。
- 投放区文档明确为手动扫描；版本升级为 1.0.1。

### 8.2 验收证据

- 静态编译：`python -m compileall -q bl_plugin_manager` 通过。
- UI/入口审计：61 个偏好属性、45 个操作符，未定义引用 0、未使用入口 0。
- 回归测试：11/11 通过。
- Blender 隔离端到端测试：161/161 通过，失败 0。
- Blender MCP 实机按钮审计：49 个操作/面板菜单入口全部实际执行，失败 0；其中兼容性按钮正确报告了一个故意注入的不兼容夹具。
- 测试结束后已恢复用户库路径 `D:\nastongbu\qitaziliao\blender_addons`、官方仓库配置及脚本目录，无临时目录残留。

## 9. 1.0.2 列表兼容性显示优化

- 列表行隐藏插件自身版本号，名称右侧改为显示当前 Blender 的兼容性结论。
- 同时显示插件声明的最高支持版本：`最高 ≤ x.y.z` 或 `最高 不限`。
- 兼容性结论沿用实时 `bpy.app.version` 判断；加载实测失败优先显示不兼容。
- MCP 安装后实测真实库 152 个列表项，样例行正确显示“✓ 当前 Blender 兼容 / ✗ 当前 Blender 不兼容”和“最高 不限”；边界状态 `≤ 5.2.0`、`≤ 5.0.0`、`不限` 均正确渲染。

## 10. 1.0.3 启动误判修复

- 修复仅凭 `bl_info`/manifest 版本范围就标记 `supported=yes` 的误判。
- 未进行真实加载测试的插件现在显示“? 声明兼容，未实测”；未声明范围的插件显示“? 未声明，未实测”。
- 真实加载成功显示“✓ 已实测可启动”；加载失败显示“✗ 启动测试失败”；声明超出当前 Blender 范围仍显示不兼容。
- 主题等无法通过 `addon_enable` 实测的条目不再伪装成可启动插件。
- 安装 1.0.3 后 MCP 实机复核：真实库中 90 个插件为“已实测可启动”、19 个为“启动测试失败”、43 个为“声明兼容，未实测”；失败样例（AI Studio、StableGen、Cats Blender Plugin）均明确显示“✗ 启动测试失败”。
- Blender 安装目录中的插件源码与当前工作区源码一致。

结论：当前真实插件库没有发现即时损坏，但代码中存在数据丢失、全局配置误改、状态过期和测试假通过等边界错误。

## 3. 修复优先级总览

| 编号 | 优先级 | 问题 | 主要风险 |
| --- | --- | --- | --- |
| FIX-001 | P0 | 回收站移动失败后永久删除插件 | 用户插件不可恢复丢失 |
| FIX-002 | P1 | 卸载挂载会删除官方扩展仓库 | Blender 官方商店配置损坏 |
| FIX-003 | P1 | 普通挂载隐式接管官方商店并开启联网 | 未提示的全局配置变更 |
| FIX-004 | P1 | 同版本文件变化不更新元数据 | 列表、兼容性和警告长期过期 |
| FIX-005 | P1 | 端到端测试发生错误仍可能返回成功 | 缺陷被测试流程漏掉 |
| FIX-006 | P2 | 挂载状态缓存无法识别路径变化 | UI 显示错误挂载状态 |
| FIX-007 | P2 | 数据库缓存暴露未保存修改 | 内存状态与磁盘状态不一致 |
| FIX-008 | P2 | 兼容性复测没有真正重载插件 | 兼容结论不可信、状态可能漂移 |
| FIX-009 | P2 | 更新替换流程缺少完整事务保护 | 更新中断后插件可能残缺 |
| FIX-010 | P3 | README 对投放区自动扫描的描述错误 | 用户预期与实际行为不一致 |
| FIX-011 | P3 | 实机核对脚本误报 `.blender_ext` | 诊断结果出现假阳性 |

## 4. 详细问题与修复方案

### FIX-001：回收站移动失败后永久删除插件

**位置**

- `bl_plugin_manager/library.py`
- `remove_plugin()`，约第 515～538 行

**现象**

插件移入 `<库>/trash/` 失败时，代码会退回执行永久删除，然后继续删除数据库记录。调用方无法区分“已移入回收站”和“移动失败后已被删除”。

**根因**

异常处理把“移动失败”错误地当作“允许直接删除”的条件：

```python
try:
    shutil.move(plugin_dir, target)
except Exception:
    shutil.rmtree(plugin_dir, ignore_errors=True)
```

**修复要求**

1. 移入回收站失败时禁止删除源目录；
2. 移动失败时保留数据库记录；
3. 返回明确的失败状态和原始错误；
4. 只有确认目标目录存在、源目录消失后，才允许删除数据库记录；
5. 操作符必须向用户报告失败，不能返回成功；
6. 如需提供“永久删除”，必须设计成独立操作并进行二次确认。

**建议接口**

将返回值改为结构化结果，例如：

```python
{
    "ok": False,
    "moved_to": "",
    "error": "移动到回收站失败：...",
}
```

**验收标准**

- 模拟 `shutil.move()` 抛出异常后，源插件目录仍完整存在；
- 数据库记录仍存在；
- UI 返回 `CANCELLED` 并显示具体错误；
- 正常移动时，目标目录存在、源目录消失、数据库记录删除。

---

### FIX-002：卸载挂载会删除官方扩展仓库

**位置**

- `bl_plugin_manager/bridge.py`
- `find_repo()`，约第 64～74 行
- `unregister_library()`，约第 237～254 行

**现象**

当前插件库的扩展目录由 `blender_org` 官方仓库使用。“卸载挂载”通过目录匹配得到官方仓库，然后直接从 Blender 偏好设置中删除该仓库。

实机检查已确认，当前卸载目标是：

```text
module = blender_org
name   = extensions.blender.org
```

**根因**

`find_repo(module=pmlib, directory=...)` 会先查找任意 `pmlib` 仓库；找不到后，再按目录匹配任意仓库。调用方没有区分：

- 插件自己创建的 `pmlib` 仓库；
- 被插件改址的 `blender_org` 官方仓库；
- 用户自行创建但碰巧指向同一路径的仓库。

**修复要求**

1. 记录挂载前官方仓库的完整配置；
2. 卸载时只删除由本插件创建的专用仓库；
3. 对官方仓库执行“恢复原配置”，不能删除；
4. 对用户自建仓库不得擅自删除或改址；
5. 找不到恢复信息时停止操作并提示用户，不能猜测默认路径。

**建议持久化字段**

在 `<库>/.pm/` 或插件偏好中保存：

- 仓库 module；
- name；
- directory/custom_directory；
- remote_url；
- enabled；
- use_custom_directory；
- use_remote_url；
- 是否由插件创建。

**验收标准**

- 卸载普通 `pmlib` 仓库时，只删除该仓库；
- 卸载被改址的 `blender_org` 时，官方仓库仍存在且恢复原配置；
- 用户自建仓库完全不变；
- 挂载、卸载、重新挂载循环三次后，不产生重复仓库。

---

### FIX-003：普通挂载隐式接管官方商店并开启联网

**位置**

- `bl_plugin_manager/bridge.py`
- `register_library()`，约第 195～234 行
- `unify_store_with_library()`，约第 87～142 行

**现象**

用户点击普通“启用/修复插件库”时，代码会优先复用 `blender_org`，重写其目录和远程地址，并把 Blender 的全局联网权限设为开启。

界面同时还提供“让官方商店使用本插件库”按钮，因此两个操作的职责冲突。

**根因**

基础挂载与官方商店统一逻辑混在 `register_library()` 中。

**修复要求**

1. `register_library()` 只负责创建或维护插件自己的本地仓库；
2. 只有用户明确执行“让官方商店使用本插件库”时，才允许修改 `blender_org`；
3. 开启全局联网权限前给出明确说明；
4. 官方商店统一操作必须可逆，并使用 FIX-002 的配置快照；
5. 已统一时重复执行应保持幂等。

**验收标准**

- 普通挂载前后 `blender_org` 配置和联网权限不变；
- 明确执行统一操作后，官方仓库才指向插件库；
- UI 的挂载状态与“官方商店已统一”状态分别显示。

---

### FIX-004：同版本文件变化不更新元数据

**位置**

- `bl_plugin_manager/library.py`
- `sync_library()`，约第 430～509 行

**现象**

同步只比较版本号。插件修改了名称、描述、作者、兼容范围、标签或 manifest，但版本号未变化时，数据库继续保留旧信息。

已通过临时插件复现：把名称从 `Old Name` 改为 `New Name`，版本保持 1.0.0，同步后仍显示 `Old Name`。

**根因**

注释声称检查“版本一致且文件未动”，实际没有保存或比较任何文件时间、大小或摘要。

**修复要求**

1. 为每条插件记录保存元数据文件指纹；
2. 传统插件至少跟踪 `__init__.py`；
3. 扩展插件至少跟踪 `blender_manifest.toml`；
4. 指纹变化时重新读取元数据和 manifest 问题；
5. 指纹未变化时才走快速跳过路径；
6. 用户字段如分类、备注、别名、收藏和自启必须保留。

**建议指纹**

优先使用：

```text
(mtime_ns, file_size)
```

如需处理时间戳保持不变的覆盖操作，可增加 SHA-256，但不建议在每次面板绘制时计算完整目录摘要。

**验收标准**

- 同版本修改名称后，刷新能看到新名称；
- 同版本修改兼容范围后，兼容结论更新；
- 同版本引入 manifest 错误后，详情出现警告；
- 用户分类、备注和别名不被覆盖；
- 文件没有变化时仍保持快速路径。

---

### FIX-005：端到端测试可能假通过

**位置**

- `_test/test_e2e.py`
- `_test/run_e2e.sh`

**现象**

Blender 执行测试脚本出现未捕获 Python 异常时，进程仍可能返回 0。测试脚本本身也只打印失败数量，没有根据失败结果执行非零退出。

审计中已经观察到 Python `PermissionError` traceback，但 Blender 进程状态仍为成功。

**根因**

运行器只读取 Blender 的进程状态码，未解析测试汇总；测试脚本没有把失败传播为进程失败。

**修复要求**

1. 测试脚本捕获顶层异常并打印统一汇总；
2. 有失败或未捕获异常时调用 `sys.exit(1)`；
3. 运行器同时检查：Blender 状态码、汇总是否存在、失败数量是否为 0；
4. 禁止用仅过滤输出的管道掩盖原始错误；
5. Windows 应提供 PowerShell 测试入口，避免依赖 Git Bash 路径转换。

**验收标准**

- 任意一条 `check()` 失败时，运行器返回非零；
- 测试脚本顶层抛异常时，运行器返回非零；
- 没有输出 `===SUMMARY===` 时视为失败；
- 所有检查通过时才返回 0。

---

### FIX-006：挂载状态缓存无法识别路径变化

**位置**

- `bl_plugin_manager/bridge.py`
- `library_state()`，约第 145～177 行

**现象**

脚本目录或扩展仓库的路径发生变化，但条目数量不变时，面板继续显示旧挂载状态。

受控测试结果：

1. 首次状态为未挂载；
2. 模拟路径变为已挂载、数量不变，普通查询仍返回未挂载；
3. `force=True` 后才返回已挂载。

**根因**

缓存代次只包含脚本目录数量和仓库数量。

**修复要求**

缓存签名至少包含：

- 脚本目录规范化路径；
- 仓库 module；
- 仓库规范化目录；
- 仓库 enabled 状态。

也可以取消这层缓存，因为集合通常很小；实际优化前应先测量面板绘制耗时。

**验收标准**

- 原地修改仓库目录后，下一次查询立即返回新状态；
- 原地修改脚本目录后，下一次查询立即返回新状态；
- 启用或停用仓库后状态同步变化。

---

### FIX-007：数据库缓存暴露未保存修改

**位置**

- `bl_plugin_manager/db.py`
- `cache_store()` 和 `LibraryDB.save()`

**现象**

保存后继续修改旧 `LibraryDB.data`，即使没有再次保存，新创建的 `LibraryDB` 也可能看到该未保存修改。

该问题已通过临时数据库复现。

**根因**

全局缓存直接持有 `self.data` 的可变引用。

**修复要求**

1. 写入缓存时保存深拷贝；
2. 从缓存读取时继续返回深拷贝；
3. 建议使用 `copy.deepcopy()`，避免 JSON 往返的额外序列化开销；
4. 增加两个数据库实例交错读写的测试；
5. 如未来引入后台定时器，需要增加写入冲突策略或修订号检查。

**验收标准**

- 保存后做未保存修改，新数据库实例仍读取磁盘中的已保存值；
- 修改从缓存读取的数据不会反向污染缓存；
- 保存后缓存与磁盘内容一致。

---

### FIX-008：兼容性复测没有真正重载插件

**位置**

- `bl_plugin_manager/operators.py`
- `PM_OT_verify_compat.execute()`，约第 353～456 行

**现象**

1. 对已经启用的插件只再次调用启用操作，并未重新加载；
2. 对原本未启用的插件，试加载成功后恢复停用，但不检查停用是否成功；
3. 无论恢复是否成功，数据库都强制写回原启用状态；
4. 未参与本轮测试的旧测试结果会被清除。

**根因**

“当前已启用”被当成“重新加载成功”，恢复步骤没有纳入测试事务。

**修复要求**

1. 默认不动已经启用的插件，可直接标记为“当前已加载”，但不能称为“复测”；
2. 若用户选择真正复测已启用插件，应明确提示会先停用再启用；
3. 每个插件测试前保存真实状态；
4. 每一步均检查返回结果和最终真实状态；
5. 恢复失败时记录为独立错误并立即提示；
6. 不清除本轮未测试插件的有效历史结果；
7. 试加载可能执行第三方插件代码，界面应明确提示风险。

**验收标准**

- 已启用插件的“复测”实际发生停用和重新启用；
- 原本未启用的插件测试后确实未启用；
- 恢复失败时数据库记录与 Blender 实际状态一致；
- 单个插件异常不会中断其余测试；
- 测试报告区分“加载失败”和“状态恢复失败”。

---

### FIX-009：更新替换流程缺少完整事务保护

**位置**

- `bl_plugin_manager/library.py`
- `_refresh_existing()`
- `replace_from_zip()`
- `backup_plugin()`

**现象**

更新会先清空当前插件目录，再复制新内容。备份失败只返回 `None`，调用方仍继续更新；复制中断时可能留下不完整插件。对已启用插件覆盖文件后，也不保证当前会话真正加载了新代码。

**修复要求**

1. 在目标同级目录完整解压和验证新版本；
2. 新版本验证通过后再进行目录原子交换；
3. 备份失败时默认停止更新；
4. 交换失败时恢复旧目录；
5. 更新已启用插件前先安全停用，交换后重新启用；
6. 重新启用失败时保留新文件，但明确报告状态，并提供回滚入口；
7. 备份名称增加微秒或随机后缀，避免同秒覆盖。

**验收标准**

- 模拟解压、复制、交换、备份任一步失败，旧插件仍可用；
- 更新过程中数据库不会提前声明新版本；
- 更新成功后当前会话实际运行新代码；
- 用户分类、备注、别名、收藏、自启标记和启用状态得到保留。

---

### FIX-010：README 对自动扫描的描述错误

**位置**

- `README.md` 的“日常：丢进去就自动管理”章节；
- `bl_plugin_manager/watcher.py`。

**现象**

README 表示文件进入投放区后几秒会自动出现，但实现明确没有注册定时器，只在用户点击“扫描投放区”时运行。

**修复选择**

二选一：

1. 保持手动扫描：修改 README 和界面文案；
2. 实现自动扫描：增加可关闭的定时器、稳定性等待、防重复处理和插件注销清理。

建议当前版本先统一为“手动扫描”，避免后台扫描半写入文件或大 ZIP 时误处理。

**验收标准**

- README、偏好设置、按钮和实际行为一致；
- 如果实现自动扫描，插件注销后不残留定时器。

---

### FIX-011：实机核对脚本误报 `.blender_ext`

**位置**

- `_mcp_test/final_check.py`，约第 26～30 行。

**现象**

脚本把扩展仓库内部索引目录 `.blender_ext` 当作非法插件目录。

**修复要求**

使用 `scan.is_ignored(e.name)` 过滤系统目录，再检查扩展模块名。

**验收标准**

- 标准 `.blender_ext` 目录不再出现在非法目录结果中；
- 真正含空格、连字符等非法名称的扩展目录仍能被检出。

## 5. 推荐实施顺序

### 第一阶段：防止数据与配置损失

1. FIX-001：回收站失败保护；
2. FIX-002：官方仓库恢复机制；
3. FIX-003：拆分普通挂载与官方商店统一；
4. FIX-009：更新事务保护。

完成这一阶段前，不建议扩大“自动更新”或“自动扫描”的使用范围。

### 第二阶段：保证状态和诊断可信

1. FIX-004：文件指纹同步；
2. FIX-006：挂载状态缓存；
3. FIX-007：数据库缓存隔离；
4. FIX-008：兼容性测试事务。

### 第三阶段：修复测试与文档

1. FIX-005：让端到端测试可靠失败；
2. FIX-011：修复诊断脚本假阳性；
3. FIX-010：统一投放区行为说明。

## 6. 回归测试清单

### 6.1 数据安全

- 回收站可写时正常移动；
- 回收站不可写时保留源目录和记录；
- 跨磁盘移动失败时不执行永久删除；
- 更新包损坏时旧插件保持完整；
- 更新中途复制失败时可恢复旧版本；
- 数据库损坏时保留 `.corrupt` 备份。

### 6.2 仓库与挂载

- 无官方仓库时挂载；
- 有默认官方仓库时普通挂载；
- 用户明确统一官方商店；
- 统一后卸载并恢复；
- 自建仓库与官方仓库并存；
- 连续挂载、卸载、重启，不产生重复条目；
- 修改仓库路径但不改变仓库数量，状态立即更新。

### 6.3 导入与同步

- 传统插件目录导入；
- 扩展插件目录导入；
- 根目录 ZIP 和单层文件夹 ZIP 导入；
- 中文、空格、点号和非法扩展模块名；
- 同版本修改名称、描述和兼容范围；
- 同 ID 扩展原地更新且不产生副本；
- 用户元数据在刷新和更新后保留。

### 6.4 启停与兼容性测试

- 未启用插件试加载后恢复未启用；
- 已启用插件真正停用、重载和恢复；
- `register()` 抛异常；
- `unregister()` 抛异常；
- 部分注册残留；
- 恢复启用状态失败；
- Blender 实际状态始终与数据库一致。

### 6.5 测试运行器

- 全部通过时返回 0；
- 单项断言失败时返回非零；
- Python 顶层异常时返回非零；
- Blender 崩溃或被终止时返回非零；
- 缺少汇总输出时返回非零；
- 临时目录始终清理。

## 7. 完成定义

一次修复版本只有同时满足以下条件，才可以视为完成：

1. P0、P1 问题全部有自动化回归测试；
2. 端到端运行器能够可靠传递失败状态；
3. 在隔离 Blender 配置中完成完整测试；
4. 在真实 Blender 中完成只读状态核对和 UI 冒烟测试；
5. 挂载、卸载、官方商店统一均可逆；
6. 任何失败路径都不会静默删除插件或数据库记录；
7. README 与实际行为一致；
8. 不覆盖用户分类、备注、别名、收藏、自启和启用状态。

## 8. 本次文档范围

本文件只整理问题和修复要求，没有修改插件实现、真实插件库或 Blender 偏好设置。
